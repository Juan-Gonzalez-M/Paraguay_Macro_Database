# Feedback implementation matrix

| Review finding or request | Implemented solution | Verification |
|---|---|---|
| Worksheet XML relationship ID returned `NA` | Namespace-safe attribute fallback plus explicit unresolved-ID error | Workbook XML inspected across all pilot files |
| Direct guard compared `source_id` to itself | Uses `.env$source_id` | Bank and finance schemas checked independently |
| EVE dates used the R epoch | Standard list-cell reader and `as_excel_date()` with Excel epoch | Smoke target: 2006-04-01 through 2026-08-01 |
| ICC character serial dates crashed | Defensive list-cell/date conversion with trapped character parsing | Smoke target: 1,236 observations |
| No plausible-date guard | Source-specific lower bounds plus reviewed future horizons (400 days normally; 1,000 for official Annex projections) | Dedicated assertions in real-workbook tests |
| YAML depended on locale | Explicit UTF-8 `readLines()` plus `yaml.load()` | Static config validation |
| No end-to-end test | Temporary full DuckDB build over every required registry source | `test-full-pipeline-smoke.R` |
| `unit` mixed unit and magnitude | Added `dim_series.scale`; FX is USD / millions | Schema migration and parser metadata |
| Publisher and format absent | Added registry and `source_files` fields | Manifest tests and metadata writes |
| Entity names unavailable | Required reference workbook populates verified legal/short names and ownership | 31 current reference entities |
| Bank/finance tables lacked semantics | Eight reference dimensions/maps and ten documented views | Mapping coverage output and thresholds |
| Existing v2 database could retain defective outputs | One-time v2 invalidation of ICC/EVE/FX observations, revisions and snapshots | Migration unit test |
| Version-1 migration named for an old schema | Added current `upgrade_v1_to_v11.R`; older filenames are compatibility aliases | Script inspection |
| Code 6200 mislabeled as USD | Added origin/unit fields; 6200 is FX-origin measured in PYG; deprecated alias corrected | Code- and description-based hard validation |
| Exact smoke-test counts become stale | Replaced equality with minimum baselines and added prior-vintage non-shrinkage checks | Real-workbook and validation tests |
| Autogenerated reference display names are fragile | Resolve by worksheet and column signature; display name only breaks ties | Synthetic renamed-table test |
| Long account IDs risk scientific notation | Force reference cells to text; retain raw IDs; reject scientific notation | Reference parser assertions |
| Full report-cell copy per vintage | Added content-addressed sheet versions and lightweight vintage links with compatibility view | Sparse-storage unit test |
| Four sources remained `inventory_only` | Added guarded documented-series parsers for all 93 Annex tables, 38 payments tables, three exchange panels and both credit-survey sheets | Source contracts and full real-workbook smoke test |
| Annex has too many heterogeneous tabs | Classified the observed period axes into eight reusable orientations and retained row/column provenance | `documented_table_catalog` plus orientation-helper tests |
| Payment and exchange semantics need official references | Load the BIC participant sheet and exchange entity/currency blocks before joining observations | Dimension thresholds and unmapped-entity guard |
| Generic parsing could overstate harmonization | Keep publisher-specific series IDs and expose unresolved units as `source_units`; no silent cross-source concordance | Unit-review warning and catalogue |
| Series identity included inferred unit/currency | Removed both attributes from the identity hash and exposed the exact `identity_basis` | Identity-invariance unit test |
| Positional collision suffix can drift | Added `identity_stability = positional` to observations, series and catalogue plus an explicit warning | Collision unit test and review query |
| Row counts cannot detect identity churn | Added prior-vintage series-set continuity with configured disappearance tolerances; failure rolls back before tombstones while preserving the full diagnostic | `documented_series_continuity` and exported CSV |
| Unit/scale could change between vintages | Continuing identities are compared across vintages; any unit/scale/currency change is an error | `documented_series_metadata_changed` |
| Totals and components coexist without hierarchy | Added `hierarchy_status`; unresolved sheets warn users not to sum all series | Sheet catalogue, series dimension and quality flag |
| Monetary table title could leak scale into an index/rate/count | Force index, percent, ratio and count to scale `units` | Metadata helper and runtime guard |
| Sheet-name exception embedded in detector | Moved `CUADRO 57a` and future overrides to `config/sheet_modes.csv` | Configuration unit test |
| Smoke test fixed the source count | Derive expected required sources from `source_registry.csv` | Full-pipeline smoke test |
| Twelve additional sources requested | Added ten documented Excel parsers/contracts and two typed long-CSV readers/contracts | Registry-derived smoke test and source-specific minima |
| Chartsheet and extreme formatted range risk | Skip non-worksheet relationships and always calculate active-cell content bounds | Interbank workbook regression test |
| Liquidity workbook contains two tables in one sheet | Added a repeated date-header block parser with block title in series identity | Source contract and duplicate-key guard |
| Compensatory-sales workbook contains future formula templates | Stop each current-year block at the last month with published component cells and reconcile total to components | Dedicated parser subtotal guard |
| Generic parsers inferred metadata once per cell | Vectorized inference over distinct metadata label/title pairs and preserved explicit parser overrides | Scalar-v7 versus vectorized equivalence test |
| Growing record list passed through a function per observation | Added lightweight `documented_record()` and direct list assignment; retained the old wrapper only for compatibility | Production-call inspection and parser tests |
| Series identity hash repeated for every period | Hash each distinct worksheet/path/frequency key once and join back in original row order | Byte-identical identity test |
| Semantic workbooks read once for raw cells and again for parsing | Store the content-addressed raw sheet from the same list-cell matrix used by the semantic parser | Single-pass architecture and full smoke test |
| Performance bottlenecks were opaque | Persist discovery, ingestion, validation, reuse and total elapsed time by source | Schema-v8 timing table and latest CSV |
| Named-table root, empty-sheet and matrix-anchor defects blocked real ingestion | Corrected XML-root handling, typed empty cells and dimension-preserving predicates | Real workbook and synthetic regression tests |
| Month/quarter classification produced invalid periods | Added `set`, rejected naked quarter digits and limited repair to validated complete quarter blocks | Helper and direct-investment workbook tests |
| Repeated rows collided on `series_id + period` | Added semantic dimensions plus value-invariant positional lanes when the source has no event ID | Real interbank, LRM and liquidity tests |
| One discovery failure aborted later sources | Moved discovery and metadata registration into the per-source error boundary | Corrupt-first/good-second pipeline test |
| Fresh bootstrap executed historical migration code | Capture the database state before schema creation and skip all invalidators only for a genuinely empty bootstrap | Fresh in-memory v11 regression |
| v9 concept cleanup referenced an undefined set | Select affected source-specific concepts before transactional deletion and preserve unrelated concepts | Selective v10 migration regression |
| Legacy v2 `dim_series` lacked current columns | Expand the complete series-column contract before any later migration or concept view runs | Existing v2 fixture plus v11 bootstrap suite |
| Annex year axis selected ordinary 1900–2100 values | Parse reviewed year-footnote forms and require an ordered sequence for every candidate year row/column | Synthetic adversarial axis and real `CUADRO 58` tests |

The original v2 architecture—content hashes, archives, source transactions, sparse events, revisions and fail-closed guards—has been retained. All supplied sources are now analytically queryable but remain publisher-specific rather than silently cross-harmonized.
